{
  "name": "server",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "bun --hot src/server.ts",
    "start": "bun src/server.ts",
    "lint": "oxlint --deny-warnings",
    "typecheck": "tsc --noEmit",
    "test": "bun test",
    "db:generate": "sh -c 'test -n \"$1\" || { echo \"Usage: bun run db:generate <migration_name>\"; exit 1; }; bun --bun drizzle-kit generate --name=$1' --"
  },
  "dependencies": {
    "contracts": "workspace:*",
    "drizzle-orm": "catalog:",
    "fastify": "catalog:",
    "ulid": "^3.0.2",
    "zod": "catalog:"
  },
  "devDependencies": {
    "@types/bun": "^1.4.2",
    "drizzle-kit": "^0.31.10",
    "oxlint": "catalog:",
    "typescript": "catalog:"
  }
}
